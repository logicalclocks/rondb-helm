# Copyright (c) 2024-2026 Hopsworks AB. All rights reserved.


{{- /*
  This Job will remove the unsupported MySQL privilege from a 22.x cluster *before* (pre-upgrade hook) running an upgrade
  We don't want to run the job on all upgrades, only if the cluster to be upgraded is < 24.x
  For this we read the Image from the mgmd Pod and extract the version.
  The admin privileges will be restored in the new 24.x cluster by the 2410_update_grant_all.yaml Job
  For more information: HWORKS-2315 CLOUD-857

  Argo CD note: under Argo, `lookup` returns nil (Argo renders with
  `helm template`, which has no cluster access by default), so `$execute`
  falls back to `.Values.mysql.force2410UserGrantMigration`. Argo customers
  upgrading from a pre-24.x cluster must set that value to true explicitly,
  and must set `mode=upgrade` so `rondb.isUpgrade` resolves true.
*/}}
{{- $execute := true }}
{{- $sts := lookup "apps/v1" "StatefulSet" .Release.Namespace .Values.meta.mgmd.statefulSetName }}
{{- if $sts }}
  {{- $containers := get $sts.spec.template.spec "containers" }}
  {{- if $containers }}
    {{- range $c := $containers }}
      {{- if eq $c.name "mgmd" }}
        {{- $imageSplits := regexSplit ":+" $c.image -1 }}
        {{- $tag := last $imageSplits }}
        {{- if semverCompare ">= 24.x" $tag }}
          {{- $execute = false }}
        {{- end }}
      {{- end }}
    {{- end }}
  {{- end }}
{{- else }}
  {{- $execute = .Values.mysql.force2410UserGrantMigration }}
{{- end }}
{{- if and $execute (include "rondb.isUpgrade" .) }}
apiVersion: batch/v1
kind: Job
metadata:
  name: revoke-set-user-id-privilege-{{ .Release.Revision }}
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": pre-upgrade
    # Explicit `before-hook-creation` aligns with Argo CD's documented
    # recommendation for named pre-upgrade hooks: ensures the previous
    # run's Job is replaced on every sync/retry rather than left to
    # collide. Helm 3 defaults to this policy implicitly; making it
    # explicit keeps Argo CD behaviour predictable.
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: 5
  parallelism: 1
  template:
    metadata:
      labels:
        rondbService: {{ include "rondb.labels.rondbService.setup-mysqld" $ }}
    spec:
      restartPolicy: Never
      serviceAccountName: {{ include "rondb.serviceAccountName" . }}
{{- include "rondb.PodSecurityContext" $ | indent 6 }}
{{- include "rondb.imagePullSecrets" . | indent 6 }}
{{- include "rondb.nodeSelector" (dict "nodeSelector" $.Values.nodeSelector.mysqld) | indent 6 }}
{{- include "rondb.tolerations" (dict "tolerations" $.Values.tolerations.mysqld) | indent 6 }}
{{- if .Values.priorityClass }}
      priorityClassName: {{ .Values.priorityClass | quote }}
{{- end  }}
      initContainers:
{{ include "rondb.container.waitDatanodes" $ | indent 6 }}
{{ include "rondb.container.waitRestore" $ | indent 6 }}
      containers:
      - name: revoke-removed-grant
        image: {{ include "image_address" (dict "image" .Values.images.upgrade2410revokegrants) }}
        imagePullPolicy: {{ $.Values.imagePullPolicy }}
{{ include "rondb.ContainerSecurityContext" $ | indent 8 }}
        command:
        - /bin/bash
        - -c
        - |
{{ tpl (.Files.Get "files/scripts/fix_admin_users_24.10.sh") . | indent 10 }}
          _revoke_privilege
        volumeMounts:
        - name: mysqld-configs
          # Using "raw", so that we can sed NodeIds into the file
          mountPath: {{ include "rondb.dataDir" $ }}/my-raw.cnf
          subPath: my.cnf
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ $.Values.mysql.credentialsSecretName }}
              key: root
      volumes:
      - name: mysqld-configs
        configMap:
          name: mysqld-configs
{{- end }}